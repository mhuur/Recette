# Outil de référence « Devis Photo » (design only) — gardé en local, hors repo
index - *.html

# Maquette design Sylva (bundle artifact Claude) — référence locale, hors repo
Recette Sylva*.html

# Téléchargements / sauvegardes éventuels
*.bak
__*

# Sauvegardes exportées depuis l'app (Réglages ▸ Sauvegarde .json) : elles
# contiennent TOUT le carnet. Jamais dans le dépôt — il est sur GitHub.
*backup*.json
carnet*.json
data/carnet*.json
data/*.json

# Firebase Hosting — cache de déploiement local
.firebase/

# Copies de conflit OneDrive (« nom (2).ext »)
* (2).*
* (3).*
